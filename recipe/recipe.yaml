schema_version: 1

context:
  version: "1.1.2"

package:
  name: livekit
  version: ${{ version }}

source:
  # The build requires multiple submodule (at least two repos deep)
  - git: https://github.com/livekit/python-sdks.git
    tag: rtc-v${{ version }}
    submodules: true

build:
  number: 1
  python:
    version_independent: true

requirements:
  build:
    - ${{ stdlib('c') }}
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
    - cargo-auditable
    - libprotobuf
    - pkg-config
  host:
    - python      ${{ python_min }}.*
    - python-abi3 ${{ python_min }}.*
    - setuptools >=61
    - wheel
    - requests
    - pip
  run:
    - python >=${{ python_min }}
    - protobuf >=4.25.0
    - types-protobuf >=3
    - aiofiles >=24
    - numpy >=1.26

tests:
  - python:
      python_version:
        - ${{ python_min }}.*
        - "*"
      imports:
        - livekit
        - livekit.rtc
        - livekit.rtc.resources
      pip_check: true

about:
  summary: Python Real-time SDK for LiveKit
  license: Apache-2.0
  license_file:
    - LICENSE
    - livekit-rtc/rust-sdks/livekit-ffit/THIRDPARTY.yml
  homepage: https://pypi.org/project/livekit/
  repository: https://github.com/livekit/python-sdks/
  documentation: https://docs.livekit.io

extra:
  recipe-maintainers:
    - xhochy
